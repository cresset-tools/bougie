[package]
name = "macos-sandbox-sys"
version = "0.1.0"
description = "FFI bindings for Apple's Sandbox framework (sandbox_init_with_parameters)"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
