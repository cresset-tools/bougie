[package]
name = "bougie-version"
version.workspace = true
edition = "2024"
description = "PHP version + composer-constraint grammar used by bougie."
license = "EUPL-1.2"
repository = "https://github.com/cresset-tools/bougie"
rust-version = "1.95"
authors.workspace = true

[dependencies]
bougie-semver = { workspace = true }

[dependencies.eyre]
version = "0.6"

[lints]
workspace = true
