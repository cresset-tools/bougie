This project's PHP interpreter, extensions, Composer dependencies, and dev services (database, cache, search, queue) are managed by bougie — never invoke php, composer, or vendor/bin tools directly. Use whenever working in a bougie project (a bougie.toml, a vendor/bougie/, or a composer.json): check whether the site is already up before running anything that needs it, and ask the user before bringing it up.
